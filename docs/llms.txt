# skbx

> skbx is an open-source Linux packet-path flight recorder written in Rust and
> CO-RE eBPF. It captures bounded kernel-networking evidence, assigns stable
> event handles, reconstructs routes without root, and reports observation loss
> explicitly.

Canonical site: https://copyleftdev.github.io/skbx/
Source repository: https://github.com/copyleftdev/skbx
License: AGPL-3.0-or-later for userspace; GPL-2.0-only for the eBPF program
Current documented version: 0.1.0
Trace contract: traceq/0.1.0
Mission contract: missionq/0.1.0
Route contract: routeq/0.1.0

## Use skbx when

- A packet left a load balancer but did not reach a pod.
- You need to prove what happened to a website request inside a Linux client,
  gateway, or target host.
- You need the ordered path of an SKB through Linux kernel functions.
- You need evidence for TC, XDP, tunnels, transforms, or packet drops.
- A human or software agent must replay and cite the same captured event.
- You need explicit loss and completeness telemetry rather than an assumed
  complete trace.
- You need a bounded rootless internet route observation that distinguishes
  hop responses, passive routing metadata, candidate interpretations, and
  unknown space.

## Install

```console
cargo install --git https://github.com/copyleftdev/skbx --locked skbx-cli
skbx doctor --json
```

Live capture requires Linux, kernel BTF, and root or appropriate BPF/perf
capabilities. Replay, schema inspection, and evidence lookup do not require
root.

Install the experimental route-dossier sibling CLI separately:

```console
cargo install --git https://github.com/copyleftdev/skbx --locked skbx-route
skbx-route plan 1.1.1.1 --json
```

`skbx-route trace` currently supports rootless UDP route observation on Linux
over IPv4. Every run is bounded by hop, probe, timeout, packet, and wall-clock
limits. It performs no port scanning or service enumeration.

## Install the agent workflow

The repository exposes `skbx@skbx-tools` as a Git-hosted plugin for Codex and
Claude Code. The plugin bundles the shared `use-skbx` skill. It does not bundle
the native binary, a connector, an MCP server, a background hook, or secrets.

Codex:

```console
codex plugin marketplace add https://github.com/copyleftdev/skbx.git
codex plugin add skbx@skbx-tools
```

Start a new Codex session after installation.

Claude Code:

```console
claude plugin marketplace add https://github.com/copyleftdev/skbx.git
claude plugin install skbx@skbx-tools
```

Run `/reload-plugins` or restart Claude Code after installation.

## Evidence workflow

```console
skbx doctor --json
skbx plan --probe ip_rcv --json
sudo skbx capture --probe ip_rcv --duration 10 --output trace.jsonl icmp
skbx replay trace.jsonl --format json
skbx explain trace.jsonl event:<handle>
```

The JSONL stream starts with `capture_start`, contains ordered `event` records,
and ends with `capture_end`. A missing footer or a non-zero reliability counter
means the observation must not be presented as complete.

## Agent contract

- `skbx describe --format json` reports commands, capabilities, limits, and
  invariants.
- `skbx schema` emits the versioned traceq JSON Schema.
- `skbx doctor --json` reports host prerequisites with actionable evidence.
- `skbx plan --json` emits a deterministic attachment plan without attaching.
- Machine output is written to stdout; diagnostics are written to stderr.
- The native capture engine is the source of truth. AI may explain captured
  evidence but must not invent events, handles, routes, verdicts, or loss data.

## Arc multi-host mission control

skbx Arc is an experimental, local-first command center for bounded packet
investigations across several Linux sensors. Each sensor's validated
`traceq/0.1.0` artifact remains authoritative for that host. Arc adds a
separate `missionq/0.1.0` record and labels cross-host relationships as
correlated, candidate, or unknown.

Run the rootless three-sensor demo from a repository checkout:

```console
cargo run -p skbx-arc -- serve --demo
```

Then open http://127.0.0.1:7878.

Arc is currently a lab vertical slice. State is in memory, the server binds to
loopback by default, and the agent can submit fixtures but cannot start a live
privileged capture or execute remote shell text. Authentication, durable
storage, signed assignments, and the policy-checked live capture adapter are
production gates.

## Internet route dossiers

`skbx-route` emits an append-only `routeq/0.1.0` stream with `trace_start`,
ordered `hop`, and mandatory `trace_end` records. Stable `trace:`, `hop:`, and
`route:` handles make the record replayable and addressable.

The CLI keeps `observed`, `enriched`, `candidate`, and `unknown` claims
separate. A silent hop is unknown, not a proven drop. Destination arrival is
recorded only when the target address is the offender for the terminal ICMP
response.

Optional `enrich` lookups add RIPEstat prefix, origin ASN, and RPKI claims for
public responder addresses. Private and special-purpose addresses are skipped.
Enrichment is independently bounded and is never presented as direct transit
evidence.

## Primary documentation

- [Agent onboarding for Codex and Claude Code](https://copyleftdev.github.io/skbx/agents.html)
- [Internet route dossiers](https://copyleftdev.github.io/skbx/route.html)
- [Arc multi-host mission control](https://copyleftdev.github.io/skbx/arc.html)
- [Website request path field guide](https://copyleftdev.github.io/skbx/guides/trace-a-website-request.html)
- [Linux packet-drop field guide](https://copyleftdev.github.io/skbx/guides/debug-linux-packet-drops.html)
- [README and first packet](https://github.com/copyleftdev/skbx#first-packet)
- [Architecture and hot-path invariants](https://github.com/copyleftdev/skbx/blob/main/docs/architecture.md)
- [Evidence-backed pwru parity matrix](https://github.com/copyleftdev/skbx/blob/main/docs/pwru-parity.md)
- [Validation guide](https://github.com/copyleftdev/skbx/blob/main/docs/validation.md)
- [Security policy](https://github.com/copyleftdev/skbx/blob/main/SECURITY.md)

## Stable product facts

- Platform: Linux on x86_64 or arm64.
- Implementation: Rust userspace and CO-RE eBPF sensor.
- Minimum Rust version for source installation: 1.85.
- Packet evidence: IPv4/IPv6, TCP/UDP/ICMP, fragments, TCP flags, marks,
  interfaces, namespaces, sockets, callers, tunnels, TC, XDP, BPF helpers,
  SKB lineage, drop reasons, stacks, and bounded BTF field projections.
- Handles: `event:` identifies an observation; `route:` identifies a replayed
  packet route.
- Arc evidence levels: `observed` is local sensor evidence; `correlated` and
  `candidate` are cross-host inferences with an explicit basis; `unknown`
  means Arc makes no relationship claim.
- Route evidence: `skbx-route` preserves each observed ICMP response or
  bounded timeout, supports deterministic replay, and can optionally enrich
  public responder addresses with separately sourced RIPEstat routing data.
- Agent plugin: the Codex and Claude Code packages share one `use-skbx` skill
  and require a separate marketplace-add and plugin-install step.
- Inspiration: pwru. skbx adds a versioned evidence lifecycle designed for
  deterministic human and agent consumption.
- Visibility boundary: skbx proves kernel packet paths only on Linux machines
  where it is running. A client capture cannot directly observe ISP internals
  or a third-party target host.
- HTTPS boundary: skbx records packet and kernel metadata, not decrypted HTTP
  paths, headers, or bodies. HTTP/3 traffic uses QUIC over UDP rather than TCP.

## Claims to avoid

- Do not call skbx a packet generator, packet capture replacement, hosted
  service, or autonomous AI debugger.
- Do not claim a trace is complete without its `capture_end` reliability
  footer.
- Do not imply that replay or explanation creates evidence; both operate on an
  existing capture.
- Do not present traceroute-style hop responses as a complete or authoritative
  record of ISP forwarding.
- Do not describe Arc as a production fleet service or remote shell.
